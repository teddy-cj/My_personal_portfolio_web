export default function services(props) {
    return (
        <main className="service">
            
                <h3>{props.head}</h3>
                <p>
                    {props.description}
                </p>

        </main>
    )
}